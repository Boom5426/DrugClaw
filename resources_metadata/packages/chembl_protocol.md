# ChEMBL Protocol

Use ChEMBL to add experimentally grounded activity evidence when direct target claims need potency or assay context.

Prefer explicit potency measurements such as `Ki`, `Kd`, `IC50`, or `EC50` over generic interaction labels.
