# DailyMed How-To

Start with the official labeling section that directly answers the query, then add any secondary context only if it improves interpretation.

When warnings or contraindications are present, keep them visible in the answer instead of collapsing them into generic safety prose.
