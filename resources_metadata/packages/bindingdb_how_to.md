# BindingDB How-To

Start with the queried drug name and extract only protein targets with direct assay support.

When multiple targets are returned, keep measured targets in the established section and defer weaker cross-database associations to secondary context.
