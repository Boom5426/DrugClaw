# Open Targets Platform How-To

For composite target plus mechanism queries, retrieve target support first and then mechanism rows.

When indication-style records appear in a target or mechanism query, keep them as supporting context instead of upgrading them into the main answer.
