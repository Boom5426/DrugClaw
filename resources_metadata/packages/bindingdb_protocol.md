# BindingDB Protocol

Use BindingDB as a primary source for experimentally measured drug-target binding or potency evidence.

Prefer `Ki`, `Kd`, `IC50`, or equivalent affinity values over looser association statements when grounding direct target claims.
