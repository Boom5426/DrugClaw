# nSIDES Protocol

Use nSIDES when adverse-effect answers need disproportionality-style signal context beyond curated label language.

Treat nSIDES outputs as statistical safety signals that support prioritization, not as confirmed incidence statements.
