# SIDER Protocol

Use SIDER for label-derived side-effect coverage when the answer needs package-insert adverse effect context.

SIDER is useful for breadth, but frequency or severity claims still need stronger supporting sources when available.
